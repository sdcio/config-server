---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: targetcleardeviations.config.sdcio.dev
spec:
  group: config.sdcio.dev
  names:
    kind: TargetClearDeviation
    listKind: TargetClearDeviationList
    plural: targetcleardeviations
    singular: targetcleardeviation
  scope: Namespaced
  versions:
  - name: config
    schema:
      openAPIV3Schema:
        description: TargetClearDeviation is the Schema for the TargetClearDeviation
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TargetClearDeviationSpec defines the desired state of Target
            properties:
              paths:
                description: Paths provide the path of the deviation to be cleared
                items:
                  type: string
                type: array
            required:
            - paths
            type: object
          status:
            properties:
              cleared:
                type: integer
              message:
                type: string
            required:
            - cleared
            - message
            type: object
        type: object
    served: true
    storage: true
