---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.1
  name: discoveryruleipranges.inv.sdcio.dev
spec:
  group: inv.sdcio.dev
  names:
    categories:
    - sdc
    - inv
    kind: DiscoveryRuleIPRange
    listKind: DiscoveryRuleIPRangeList
    plural: discoveryruleipranges
    singular: discoveryruleiprange
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DiscoveryRuleIPRange is the Schema for the DiscoveryRuleIPRange
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DiscoveryRuleSpec defines the desired state of DiscoveryRule
            properties:
              cidrs:
                description: list of CIDR(s) to be scanned
                items:
                  type: string
                maxItems: 64
                type: array
                x-kubernetes-validations:
                - message: cidr is immutable
                  rule: oldSelf.all(x, x in self)
              concurrentScans:
                default: 10
                description: number of concurrent IP scan
                format: int64
                type: integer
                x-kubernetes-validations:
                - message: concurrentScans is immutable
                  rule: self == oldSelf
              excludes:
                description: IP CIDR(s) to be excluded
                items:
                  type: string
                maxItems: 64
                type: array
                x-kubernetes-validations:
                - message: excludes is immutable
                  rule: oldSelf.all(x, x in self)
            required:
            - cidrs
            type: object
            x-kubernetes-validations:
            - message: cidr is required once set
              rule: '!has(oldSelf.cidrs) || has(self.cidrs)'
            - message: excludes is required once set
              rule: '!has(oldSelf.excludes) || has(self.excludes)'
        type: object
    served: true
    storage: true
    subresources:
      status: {}
